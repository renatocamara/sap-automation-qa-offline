# Throwaway lab environment generated by deploy-sap-sim-lab.sh.
# It may contain the lab SSH PRIVATE KEY (ssh_key.ppk) — NEVER commit this.
lab-workspace/

# SSH keys / credentials — never commit
*.ppk
ssh_key*
sap_key
id_rsa*
*.pem

# Offline bundle + build artifacts (large / machine-specific)
sapqa-offline-bundle.tar.gz
sapqa-offline-bundle.tar.gz.sha256
wheels/
collections_offline/
jump_rpms/
.buildenv/
.venv/

# Generated reports and local notes
CONFIG_*.html
SESSION-NOTES.md
CHAT-TRANSCRIPT.md

# Presenter-only teleprompter script — keep local, do NOT publish to the shared repo
SPEECH.md

# Python
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db
